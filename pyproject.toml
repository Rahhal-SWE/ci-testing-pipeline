[project]
name = "ci-testing-pipeline"
version = "0.1.0"
description = "CI pipeline demo with pytest and GitHub Actions"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
